# GenOpt Team — Start Here

1. Create a GitHub repository named:
   `SC10-Smart-Energy-Demand-and-Appliance-Scheduling-System-GenOpt`
2. Add all 3–4 members as collaborators.
3. Upload this complete starter folder.
4. Replace `[Name]` placeholders in `docs/STEP-1.md`.
5. Add your GitHub URL in `docs/STEP-1.md`.
6. Each member makes at least one meaningful commit.
7. ### Testing and Documentation Responsibility

Srishti is responsible for reviewing Step 1 test cases, checking validation evidence, and helping maintain project documentation.
8. Run:
   `pip install -r requirements.txt`
   `python src/validate_data.py`
9. Take a screenshot of the PASS result and save it under `results/step1/`.
10. For the deliberately invalid case, explain that `data/invalid_case.csv` is expected to fail the data checks; do not mix it with valid data.
11. Rehearse the 3-minute Step 1 faculty demo using the order in the guide.
